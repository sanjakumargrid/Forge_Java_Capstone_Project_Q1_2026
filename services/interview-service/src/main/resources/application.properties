# ─────────────────────────────────────────────────────────────────────────────
# TalentGrid — interview-service configuration
# ─────────────────────────────────────────────────────────────────────────────

spring.application.name=interview-service
server.port=8084

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.properties.spring.json.add.type.headers=true

spring.kafka.consumer.group-id=interview-service-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.talentgrid.*
spring.kafka.consumer.properties.spring.json.value.default.type=com.talentgrid.kafka.events.base.BaseEvent


application.service.url=http://localhost:8083

# Gemini AI — get your key from https://aistudio.google.com/app/apikey
gemini.api.key=${GEMINI_API_KEY:your-gemini-api-key-here}

# ─── Google Calendar API (REQ-ER-07) ─────────────────────────────────────────
# Path to your Service Account JSON key file downloaded from Google Cloud Console
# Setup steps:
#   1. Go to console.cloud.google.com → IAM & Admin → Service Accounts
#   2. Create a service account and enable "Domain-wide Delegation"
#   3. Download the JSON key file and set its path below
#   4. In Google Workspace Admin → Security → API Controls → Domain-wide Delegation
#      add scope: https://www.googleapis.com/auth/calendar
google.calendar.service-account-key-path=${GOOGLE_SERVICE_ACCOUNT_KEY_PATH:/etc/secrets/talentgrid-service-account.json}
google.calendar.delegate-email=${GOOGLE_CALENDAR_DELEGATE_EMAIL:admin@yourdomain.com}
google.calendar.application-name=${GOOGLE_CALENDAR_APP_NAME:TalentGrid}