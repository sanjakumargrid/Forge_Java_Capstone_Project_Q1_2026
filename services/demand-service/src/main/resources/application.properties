# ─────────────────────────────────────────────────────────────────────────────
# TalentGrid — demand-service configuration
# ─────────────────────────────────────────────────────────────────────────────

# Single source of truth: repo-root .env (../ or ../../ from module working directory)
spring.config.import=optional:file:../.env[.properties],optional:file:../../.env[.properties]

spring.application.name=demand-service
talentgrid.kafka.auth-consumer.enabled=true
server.port=${DEMAND_SERVICE_PORT:8082}

# Kafka — override KAFKA_BOOTSTRAP_SERVERS in Docker/K8s
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# Producer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

spring.kafka.producer.properties.spring.json.add.type.headers=false

# Consumer
spring.kafka.consumer.group-id=demand-service-group
spring.kafka.consumer.auto-offset-reset=latest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.talentgrid.*
spring.kafka.consumer.properties.spring.json.value.default.type=com.talentgrid.kafka.events.base.BaseEvent

# Database Connection
spring.datasource.url=${DB_URL:jdbc:postgresql://${DB_HOST:localhost}:${POSTGRES_HOST_PORT:5432}/${DB_NAME:talentgrid}}
spring.datasource.username=${DB_USERNAME:talentgrid}
spring.datasource.password=${DB_PASSWORD:talentgrid}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.com.talentgrid.demand=DEBUG
logging.level.org.springframework.kafka=INFO

jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

# Redis
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
#spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=${REDIS_TIMEOUT:60000}

# Gemini AI Configuration (primary provider)
gemini.api.base-url=${GEMINI_BASE_URL:https://generativelanguage.googleapis.com}
gemini.api.key=${GEMINI_API_KEY:}
gemini.embedding.model=${GEMINI_EMBEDDING_MODEL:models/gemini-embedding-001}
# Must match skills.embedding vector(N) in PostgreSQL; sent to Gemini as outputDimensionality
gemini.embedding.output-dimension=${GEMINI_EMBEDDING_DIMENSION:768}
gemini.chat.model=${GEMINI_CHAT_MODEL:models/gemini-2.5-flash}

# Groq AI Configuration (failover provider for text generation)
groq.api.base-url=https://api.groq.com/openai/v1
groq.api.key=${GROQ_API_KEY:}
groq.chat.model=llama-3.3-70b-versatile

# AI failover circuit breaker
ai.failover.failure-threshold=3
ai.failover.open-duration-seconds=120

# Caching TTLs
ai.embedding.skill-cache-ttl-seconds=2592000
ai.embedding.jd-cache-ttl-seconds=86400
server.error.include-message=always
server.error.include-stacktrace=always

# Search activation scheduler (APPROVED -> INTERNAL_SEARCH / OPEN_EXTERNAL)
demand.scheduler.search-activation.cron=${DEMAND_SEARCH_ACTIVATION_CRON:0 * * * * *}

# Approval SLA scheduler (24h reminder, 72h escalation + auto-close)
demand.scheduler.approval-sla.cron=${DEMAND_APPROVAL_SLA_CRON:0 0 * * * *}
demand.scheduler.approval-sla.cron=*/10 * * * * *
spring.main.allow-bean-definition-overriding=true
