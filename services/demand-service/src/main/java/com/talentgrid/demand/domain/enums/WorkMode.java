package com.talentgrid.demand.domain.enums;

/**
 * Defines the work mode for a demand (e.g., whether the candidate will work remotely, onsite, or hybrid).
 */
public enum WorkMode {
    REMOTE,
    HYBRID,
    ONSITE
}
