# ─────────────────────────────────────────────────────────────────────────────
# TalentGrid — offer-service configuration
# ─────────────────────────────────────────────────────────────────────────────

# Single source of truth: repo-root .env (../ or ../../ from module working directory)
spring.config.import=optional:file:../.env[.properties],optional:file:../../.env[.properties]

spring.application.name=offer-service
server.port=${OFFER_SERVICE_PORT:8088}

# Kafka
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.properties.spring.json.add.type.headers=true

spring.kafka.consumer.group-id=offer-service-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.talentgrid.*
spring.kafka.consumer.properties.spring.json.value.default.type=com.talentgrid.kafka.events.base.BaseEvent

# Other services
application.service.url=${APPLICATION_SERVICE_URL:http://${APPLICATION_SERVICE_HOST:localhost}:${APPLICATION_SERVICE_PORT:8086}}
candidate.service.url=${CANDIDATE_SERVICE_URL:http://${CANDIDATE_SERVICE_HOST:localhost}:${CANDIDATE_SERVICE_PORT:8085}}

# PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://${DB_HOST:localhost}:${POSTGRES_HOST_PORT:5432}/${DB_NAME:talentgrid}}
spring.datasource.username=${DB_USERNAME:talentgrid}
spring.datasource.password=${DB_PASSWORD:talentgrid}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.baseline-on-migrate=${SPRING_FLYWAY_BASELINE_ON_MIGRATE:true}

# Compatibility fixes
spring.cloud.compatibility-verifier.enabled=${SPRING_CLOUD_COMPATIBILITY_VERIFIER_ENABLED:false}
spring.mvc.pathmatch.matching-strategy=${SPRING_MVC_PATHMATCH_MATCHING_STRATEGY:ant_path_matcher}

# Swagger disabled
springdoc.swagger-ui.enabled=${SPRINGDOC_SWAGGER_UI_ENABLED:false}
springdoc.api-docs.enabled=${SPRINGDOC_API_DOCS_ENABLED:false}

# OpenAI - only needed if this service uses OpenAI
openai.api-key=${OPENAI_API_KEY:}

# =========================
# DocuSign Configuration
# =========================

docusign.integration-key=${DOCUSIGN_INTEGRATION_KEY:}
docusign.user-id=${DOCUSIGN_USER_ID:}
docusign.account-id=${DOCUSIGN_ACCOUNT_ID:}
docusign.base-path=${DOCUSIGN_BASE_PATH:https://demo.docusign.net/restapi}
docusign.private-key-path=${DOCUSIGN_PRIVATE_KEY_PATH:docusign/private.key}
docusign.auth-server=${DOCUSIGN_AUTH_SERVER:account-d.docusign.com}
docusign.enabled=${DOCUSIGN_ENABLED:false}

# Must match JWT_SECRET in repo-root .env
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

talentgrid.kafka.auth-consumer.enabled=true
